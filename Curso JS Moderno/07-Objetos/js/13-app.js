const product = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible: true,
}
// objecto de la derecha
console.log(Object.keys(product));
// objectos de la left (el valor que tiene)
console.log(Object.values(product));

// Todos los Objectos
console.log(Object.entries(product));